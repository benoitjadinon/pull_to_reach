name: pull_to_reach
description: Pull to reach implemention for TiO
version: 0.0.1
author: Julian Bissekkou
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: